import 'package:flutter/material.dart';

/// Widget extraído de gestao_screen.dart
///
/// Futuro destino da classe CaixasHeroHeader.
///
/// Nesta fase o arquivo é apenas um placeholder seguro para a refatoração.
class CaixasHeroHeaderPlaceholder extends StatelessWidget {
  const CaixasHeroHeaderPlaceholder({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
